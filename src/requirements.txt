# Backend (FastAPI + ZMQ + Pydantic)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pyzmq>=25.0

# Screen process (pygame + QR code generation)
pygame>=2.5
qrcode[pil]>=7.4

# Audio (Phase 7) — sounddevice wraps PortAudio for cross-platform playback
sounddevice>=0.4
soundfile>=0.12
numpy>=1.24

# HTTP client (for LLM API calls, Phase 6)
httpx>=0.27

# Prompt templating (Phase 6, optional; falls back to {{var}} substitution)
Jinja2>=3.1

# TOML parsing (only needed for Python < 3.11; stdlib tomllib otherwise)
tomli>=2.0; python_version < "3.11"

# Sensing process already uses these (Phase 5 wiring):
# numpy, scipy, scikit-learn, spidev, lgpio
